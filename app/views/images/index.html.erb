<%= render 'shared/nav' %>
<h1>Gallery</h1>
<% @images.each do |image| %>
  <div class="image">
    <%= link_to image_tag(image.image_path.url(:thumb)), image_comments_path(image) %>
  </div>
<%end%>


